import { GeoPose } from "../GeoPose.js"
import { Position } from "../Position.js"

/** An experimental version of GeoPose that combines the elements in a more open-typed way. */ 
export class NeoPose extends GeoPose {

	/** The position of the  */
	// private _position: Position;

	// get quaternion(): any { return null; }
	// set quaternion(values: number[]): any {}


	// get YPRAngles(): any { return null; }
	// set YPRAngles(values: number[]): any {}
}
